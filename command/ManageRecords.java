package command;

public class ManageRecords {
    
}

/* 
public void addRecord(BookRecord newRecord) {
        bookCollection.add(newRecord);
        recordCount++;
    } 
    //removeRecord()
    public void removeRecord(String isbn) {
        for (BookRecord targetRecord: bookCollection) {
            if (targetRecord.getIsbn().equals(isbn)) {bookCollection.remove(targetRecord);}
        }
    }
    //updateRecordInfo()
    public void updateRecordInfo(String isbn, BookRecord.Info targetInfoField, String newValue) {
        for (BookRecord targetRecord : bookCollection) {
            if (targetRecord.getIsbn().equals(isbn)) {targetRecord.changeInfo(targetInfoField, newValue);}
        }
    }
*/
